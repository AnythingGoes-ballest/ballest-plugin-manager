[meta]
name = "Plugin Manager"
version = "0.6.0"
author = "AnythingGoes"
description = "The plugins button in the footer, the plugin browser (install, update, turn off, remove) and the console."
essential = true

[script]
files = ["main.as"]
timeout = 50
