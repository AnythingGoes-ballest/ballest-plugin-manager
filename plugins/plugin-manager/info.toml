[meta]
name = "Plugin Manager"
version = "0.4.1"
author = "AnythingGoes"
description = "The plugins button in the footer, the plugin browser (install, update, remove) and the console."
essential = true

[script]
files = ["main.as"]
timeout = 50
